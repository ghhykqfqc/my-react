import React from 'react';
import Header from '../../components/Header';

function RightRelate () {
  return (
    <div>
      <Header />
      {/* 其他页面内容 */}
      右因子关联表
    </div>
  );
}

export default RightRelate;